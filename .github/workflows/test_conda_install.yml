on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  example-1:

    runs-on: ubuntu-latest
    steps:
      - uses: goanpeca/setup-miniconda@v1
      - name: Conda info
        shell: bash -l {0}
        run: |
            python --version
            conda info
            conda --version
